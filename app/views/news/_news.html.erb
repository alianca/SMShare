<div class="news-item">
  <div class="date">
    <%= news.created_at.strftime("%d") %>
    <span class="month">
      <%= news.created_at.strftime("%b") %>
    </span>
  </div>
  <h1 class="title"><%= news.title %></h1>
  <span class="news-text">
    <%= news.short.html_safe %> <%= link_to "Ler mais...", news_path(:id => news.id) %>
  </span>
</div>
